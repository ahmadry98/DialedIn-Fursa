apiVersion: 1

providers:
  - name: DialedIN dashboards
    orgId: 1
    folder: DialedIN
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
